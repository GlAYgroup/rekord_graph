# rekord_graph

DJトランジション・ナレッジベース。Notion に記録した「曲Aのどのキュー → 曲Bのどのキュー」を、
スマホ/PC から即座に引けるようにする。設計の全体像は `docs/PROPOSAL.md`。

---

## ★ 最重要の前提: キュー名が正、アルファベットは信用しない

**Notion の `「」` の中身（キュー名）が唯一の正準情報。**
**アルファベット（A〜P のホットキュー記号）はズレていることが多い。** これは例外ではなく常態。

```
ベノム：F「助走 1サビ終受け 」
        ↑ ズレうる    ↑ こちらが正
```

### なぜズレるのか

rekordbox 側でキューを頻繁に編集し、「メモリーキュー→ホットキュー変換」を実行すると
記号の割り当てが繰り下がる。Notion のメモは変換前の記号のまま残る。
実測（2026-08-16）: キュー名が完全一致した14件のうち **9件で記号がズレ、9件すべてが「Notion が実機より1つ手前」**。

さらに 2026-08-25 まで**ツール側にも「D 以降が1つ後ろにズレる」バグがあった**:
master.db の `Kind` は **4 を飛ばして 5=D, 6=E…** なのに、素朴に 1..16=A..P と読んでいた
（実測: 573件中 Kind=4 は0件。`tools/rb_export.py` で修正済み）。過去の「1つ手前」の実測は
このバグと編集起因のズレが混ざったもの。今後も記号を信用しない原則は変わらない。

### 実装・調査時のルール

1. **照合は必ずキュー名を一次キーにする。** 記号は同名キューが複数あるときのタイブレークにのみ使う
2. **記号の不一致をエラーにしない。** 「実機 G / Notion F」と両方見せて、人の承認を待つ
3. **rekordbox 実機の値が唯一の正。** 表示する記号は必ず rekordbox 由来のものにする
4. **キュー名の照合は正規化してから。** NFKC・空白除去は必須。実データには
   末尾スペース（`'助走 1サビ終受け '`）、語順違い（`ピアノ前 ビート` vs ` ビート ピアノ前`）がある
5. 名前が変わっても **rekordbox のキュー UUID は不変**。差分検出は必ず UUID で行い、名前推測に頼らない
6. ただし **キューを作り直すと UUID は変わる**（消して打ち直す / メモリーキュー→ホットキュー変換）。
   そのとき頼れるのは **位置（ms）**。名前も記号も変わるが、曲の中のどこを指すかは変わらない
   （実測: メモリーキュー192件すべてが同じ位置のホットキューを持つ = 変換で 1ms も動かない）。
   位置は曲の中で一意ではない（54曲中24曲で重複あり）ので、**位置だけで決め打ちしない**

---

## 「ズレを直して」と言われたときの手順

**`/sync-cues` スキル**（`.claude/skills/sync-cues/SKILL.md`）に手順がある。必ずそれを踏む。
要点だけ:

```bash
./.venv/bin/python tools/sync.py --dry-run
```

`tools/sync.py` が rekordbox と Notion 📍Cues を **キュー UUID** で突き合わせ、変更を分類する。

| 検出 | 扱い |
|---|---|
| UUID が変わった（作り直し） | **位置を手掛かりに同じ行として結び直す**（★最重要。行を作り直さないので 🔀Transitions の参照が切れない）。決め手は ①位置＋キュー名 ②同名が複数なら記号 ③位置が一意 ④位置＋キュー名の前方一致 ⑤キュー名が一意 の順。どれでも決まらなければ推測せず追加/削除のまま出す |
| 記号が変わった | 修正を提案（★付きで表示。DJ 中に押すボタンが変わるため最重要） |
| ループになった / やめた | 修正を提案。**判断の元は実機の「終わりの位置（OutMsec）が入っているか」**。`BeatLoopSize` はビートループで作った分にしか入らない（実測: ループ54件中15件だけ）ので使わない |
| キュー名が変わった | 修正を提案 |
| 位置だけ変わった | 通知のみ。書き込まない（ループの終わりだけ動いた場合も同じ） |
| UUID が消えた | 削除として提示。**書く前に 🔀Transitions の参照を必ず確認**（参照ありは結び直すか保留） |
| 新しい UUID | 追加として提示。曲が 🎵Tracks に無ければ「曲追加」を先に出して行を作る |
| 曲名・アーティスト・ジャンル等が変わった | **「曲更新」として提示**。🎵Tracks は鏡なので実機の値で上書きする。これが無いと、実機で曲名を整えても Notion に一生届かない（`track_add` は新規作成しかしない。実測 2026-09-09: 71件中57件が古いまま残っていた）。BPM・長さは**数として比べる** — 文字列で比べると `160` と `160.0` が毎回偽の差分になる |
| 曲が rekordbox から消えた | 「曲削除」として提示。ただし**キュー行が残っている / 🔀Transitions から参照がある間は保留**で出し、書き込まない（リネームで置き換わった古い行がアプリに「0キュー」の重複として残るのを防ぐ） |

- **既定は「何もしない」。** `--dry-run` を外すと1件ずつ y/n/a/q で聞く。承認した分だけ書き込む
- 📍Cues の `ループ` / `ループ終ms` 列は、**書き込む直前に sync が無ければ生やす**（`ensure_loop_columns`）。
  人が Notion を触る必要はない。列が無いうちはアプリ側が「ループではない」として動く
- 保留したものは次回の sync でまた出る
- **一括で書き込む前に rekordbox を終了させる。** 起動中でも読めるが、
  投入中にキューが書き換えられると Notion 側に古い UUID が残る（実際に発生した。作者ローカルの `data/LOAD_STATE.md` に記録）

### 初回だけ必要なセットアップ

MCP 経由のクエリにはワークスペースの利用上限があるため、sync は Notion 公式 API を直接叩く。
手順は `README.md` の「セットアップ」。要点:

- トークンと database ID は **リポジトリの外** `~/.config/rekord_graph/config.json`
  （または環境変数 `NOTION_TOKEN` / `NOTION_DB_*`）。読むのは `tools/config.py` と `web/src/lib/notion.ts` だけ
- DB は `tools/setup_notion.py --parent <ページ> --write` が作る。**列名の正本はこのスクリプト**
  （アプリ・sync が参照する名前なので、列を足すときはここにも足す）

---

## 移行（手書きメモ → Transitions）

```bash
./.venv/bin/python tools/migrate_transitions.py          # 解析して報告
./.venv/bin/python tools/migrate_transitions.py --apply  # 解決できた分を投入
```

- **自動確定するのは「キュー名が完全/前方一致」かつ「候補が一意」のときだけ。**
  部分一致や記号だけの一致は実際に誤マッチを生んだので、必ず人に確認する
- 人が判断した対応付けは `data/migration_overrides.json` に理由つきで残す。
  自動解決より優先されるので、再実行しても同じ結果になる
- 曲名が複数候補でも、**キュー名が一意ならそれで曲が確定できる**
  （「メルト」は2曲あるが「助走 受け」を持つのは Nurecha 版だけ）

---

## データの持ち方

- **正準ID**: 曲 = rekordbox の `ContentID` / キュー = rekordbox の `UUID`。Notion の曲名は正準ではない
- **🎵 Tracks / 📍 Cues は機械が管理する鏡。** 手編集しても次の同期で上書きされる。編集は rekordbox 側で行う
- **🔀 Transitions だけが人の入力先。** 選択肢が rekordbox 由来なので、新規入力では記号がズレない
- 曲名には表記ゆれが多い（`ドーナルホール`/`Rabit Hole`/`マーシャル`、`アイドル`⇄`Idol`）。
  対応表は `data/aliases.json`（個人データ。git には入れない）。**未解決の曲名で勝手にノードを作らない** — 必ず可視化して人に聞く
- 同じ曲名に rekordbox 側の候補が複数ある場合がある（`メルト` `転生林檎` `Rabbit Hole` `フォニイ`）。
  **キュー名が曖昧性を解消する**ので、曲名だけで決め打ちしない

### Notion データベース

4 つの DB の ID は `~/.config/rekord_graph/config.json`（人ごとに違うのでコードに書かない）。
MCP から叩くときの data_source_id は memory に置いてある。

**🗺️ Layouts はアプリが書き込む唯一の DB。** グラフの配置パターン（「パターン1」…）を置く。
rekordbox とは無関係なので sync とは競合しない。人が手で編集する場所ではない。

---

## 開発

- `tools/` = ローカル実行の Python（rekordbox 連携）。`./.venv/bin/python` を使う
- `web/` = Next.js アプリ。`pnpm --dir web dev`。**Vercel からは `master.db` が読めない**ので、
  rekordbox 連携は必ずローカル実行。アプリは Notion を読むだけの読み取り専用にする
- MVP では DB（Supabase 等）を増やさない（`with-supabase` スターターを使わないのはこのため）

### web/ の約束

- **見た目の正本は `web/DESIGN.md`、値の正本は `web/src/app/globals.css` のトークン。**
  画面側で色・余白をハードコードしない
- **Notion アクセスは `src/lib/notion.ts` だけ。** `NOTION_TOKEN` をクライアントに出さない
  （`NEXT_PUBLIC_` を付けない）
- **クライアントから `src/lib/graph.ts` を import しない**（`server-only` を抱えている）。
  表示用の変換は `src/lib/format.ts` に置く
- **本番用の「パフォーマンスモード」がある**（`src/components/PerformanceMode.tsx`）。
  プレイ中は編集の入口を全部畳む: 入口には `data-edit` を付け、`globals.css` が
  `[data-perf="on"] [data-edit]` を消す（サーバで描いた画面も一律に畳める）。
  **CSS で止まらない書き込みだけは JS で止める** — グラフのノードは指が当たると動き、
  1.2秒後に配置が Notion へ保存されるため、`GraphExplorer` が自分で見て止めている
- **キューの短い表し方（`F「1サビ終」`）は `lib/format.ts` の `cueLabel` が唯一の正本。**
  同じ組み立てを画面ごとに書かない（ループの印のような修飾が片方だけに出るため）。
  ループなら「ループ」を添えるが、**キュー名が既に「ループ」と言っていれば添えない**（キュー名が正）
- **🔀Transitions の `小節数` は「次の曲（To）のキューの何小節前から繋ぎ始めるか」。**
  From 側の長さではない。繋ぎ始めが To キューより**後**のこともあるので、
  `小節数（後）` 列が対になっている。**入るのは片方だけ** — 入力画面が片方の欄を塞ぎ、
  API も両方入りを 400 で弾く（「何小節ずらすか」の答えが2つある行を作らない）。
  表示（`次の曲 C「歌入り」の16小節前` / `…の8小節後`）は `lib/format.ts` の
  `barsLabel` が唯一の正本で、「16小節」とだけ出さない（逆向きに読めるため）。
  出す・出さないの判断も **`barsLabel` の結果**で行う — `bars != null` で出し分けると
  「後」だけ入った繋ぎが表示ごと消える
- **繋ぎには「要練習」マークが付けられる**（🔀Transitions のチェックボックス列。
  無ければ最初のマーク時にアプリが生やす）。付け外しは曲ページ・グラフ・`/practice`・
  **`/play`（下見中だけ）** の1タップトグル（`PracticeToggle`。星の `RatingPicker` と
  同じ楽観更新）と、**`/new` の入力画面**（入れながら「これは練習が要る」と一手で残せる）。
  マークした繋ぎの一覧が `/practice`（ナビ「練習」）
- **アプリが書き込むのは 🗺️Layouts と 🔀Transitions の2つだけ。**
  `/new` が繋ぎの入力画面で、From曲を選ぶとその曲のキューだけがパッドで並ぶ。
  **`/new?from=<曲ID>` / `?to=<曲ID>` で曲を埋めた状態で開ける** — 曲ページとグラフの
  選択パネルから来る入口。**`?edit=<繋ぎID>` なら編集モードで開く** — 曲ページ・グラフの
  各繋ぎの「編集」から来る（どのキュー同士を結ぶかを付け替える口）。
  **編集モードでは保存ボタンの隣に「この繋ぎを削除」が出る** — 入口（曲ページ・グラフ・
  `/practice`）に削除が無いので、そこから来た人が一覧で同じ行を探し直さずに消せる。
  出すかどうかは `editingId` ではなく**一覧にその行が在るか**で決める（他の画面で消された
  行に DELETE を投げないため）。
  知らない ID は 404 にせず未選択で開く（実際の検証は API 側）。
  書き終えたら `?edit=` は URL から外す（再読み込みでまた編集モードにしない）。
  保存・削除のあとは `router.refresh()` でサーバの一覧を取り直す —
  画面に出したままの行が Notion とズレるのが、入力後にいちばん困る
  （Notion のリレーションは候補を他プロパティで絞れないので、ここはアプリ側でしか作れない）。
  `properties()` は全列を書くので、書き込みの前に `ensureColumns()` が
  **足りない列だけ**を生やす（人が Notion を触らずに済む）。
  アプリが作った行は `出典 = アプリ`。移行分（`完全版`）と区別が付く。
  書き込み後は `revalidateTag("notion")` でキャッシュを捨て、すぐグラフに出す
- 曲の表示名は **「曲名 (リミックス)」**（`ベノム (Gakui Bootleg)`）。原題はファイル名由来で
  読めないので、別名（無ければ原題から装飾を削ったもの）＋括弧内のリミックス名に整える
  （`src/lib/graph.ts` の `displayName`）。整えた結果が衝突する曲だけ原題の断片を足す。
  **出した名前は省略記号で刈らない**（長いものは折り返す）
- **`/play` がプレイ中に開く画面。** 「今かけている曲」から繋げる先を並べ、タップすると
  その曲が「今かけている曲」になる、の繰り返し。**キュー名もメモも省略しない**
  （プレイ中に読めない情報は無いのと同じ）。かけてきた順も履歴も端末（localStorage。
  読み書きは `src/lib/playlog.ts` だけ）が持つ — その場の話なので Notion には残さない。
  **本番中（`usePerformance`）は一度かけた曲を使い切りにする**: 使った曲へ入る繋ぎは
  一覧から消え、「この先◯曲」も残っている曲だけで数え直す。この画面用のモードは
  作らない（同じ問いに答えが2つある状態を作らないため）。
  **並びは「この先◯曲」が多い順**（同数ならテンポが近い順）。本番中は数が減っていくので
  並びも1タップごとに入れ替わる。
  **下見中（本番ボタンを押していないとき）だけ、各カードの下に「要練習」トグルが出る** —
  下見は「ここは練習が要る」と気づく時間なので、入力画面へ戻らせない。本番中は出さず、
  代わりに印だけを右に出す（同じことを1枚のカードで2回言わない）。
  トグルは曲送りボタンの**外**に置く（ボタンの中にボタンは置けず、入れるとタップも食われる）。
  **記録に無い曲へも移れる**（本番では急に別の曲へ繋ぐため）: 「曲を変える」で全曲の
  一覧が出て、選んだ曲は**かけてきた順の後ろに足される** = 繋いだ曲として数える
  （繋ぎが1本も無い曲も一覧に並ぶ。かけた曲は印を付けて後ろに回すが、外しはしない）。
  一区切りにして選び直すのは「リセット」で、**一番下・2タップ**（「戻す」の隣に置くと
  暗いブースで誤爆してセットが終わる）。**「戻す」「曲を変える」「リセット」に
  `data-edit` を付けない** — Notion に何も書かないうえ、本番中に畳むと
  急な差し替えから戻る道が無くなる
- **セットが終わると履歴に残る**（リセット、または `/play?from=` で別の曲から開き直したとき。
  2曲以上つないだものだけ・最大50セット）。一覧は **`/play/history`**（入口は
  リセットの隣と、リセット直後に立つ「最初にかける曲」の**一覧より上**）。
  そのため1手は **`{ かけた曲, そこへ入るのに使った繋ぎID }`** で持つ（`rg.play.v2`。
  同じ2曲の間に繋ぎが複数あるので、後から曲の組では引き直せない）。
  「曲を変える」で移った所は繋ぎID が無い = 履歴でも**繋ぎとして書かない**。
  曲や繋ぎが消えていても**その1手は落とさず**「不明な曲」として出す。
  **「1回だけ走る」見張りは state ではなく ref**（`setState` は次の描画まで効かないので、
  effect が2回走ると同じセットが2つ積まれる。実際に起きた）
- **ホーム画面に置ける（PWA）。** マニフェストは `src/app/manifest.ts`、開いた先は `/play`。
  アイコンの元データは `web/public/icon-source.svg`（書き出しは
  `qlmanage -t -s <px> -o . icon-source.svg`）。`public/sw.js` は**ネットワーク優先**で、
  取りに行って失敗したときだけ最後に見た画面を出す — 回線が細いブースで
  古い記録を掴まないことを、オフライン対応より優先する。`/api/` と GET 以外は素通し
- **「あと何曲つなげるか」の探索は `lib/route.ts` の1実装だけ。** サーバ（全曲ぶん）と
  端末（使った曲を外して数え直す）で入口が違うだけ。`maxOnwardFrom` は `Graph`
  （server-only）を要求しないので client から呼べる
- **ツリー（`/graph?mode=tree`）は文字を刈らない。** 折り返しは `lib/tree.ts` が
  確定させ、画面は行をそのまま描く（測る場所と描く場所が分かれると必ずはみ出す）。
  繋ぎのラベルは線の中点ではなく**行き先カードの左横**に置く — 中点だと兄弟同士の
  間隔が行間隔の半分になり、複数行だと必ず重なる。最初に見せるズームには下限があり、
  全体が入らないときは起点を左端に置く（⊡ が全体表示）
- **グラフの選択パネルの並びは「この先つなげる曲数」が多い順**（同数ならテンポが近い順 →
  曲名 → ID）。`/play` の一覧と同じ問いなので同じ答えを出す。数は行ごとに
  `最大◯曲` として出す（サーバで前計算した `otherMaxFrom`。並びの根拠が画面に無いと読めない）
- **グラフの配置はサーバで計算する**（`src/lib/layout.ts`）。端末ごとに力学を回すと
  丸め誤差で別の形に落ち、スマホと PC で違う形になる。座標を props で渡すこと。
  配置計算は「同じ入力なら同じ座標」が絶対条件 = ノードは id 順、エッジは
  source/target/id 順に並べてから回す（並行エッジの同点順で誤差が変わるため）。
  ノードは**ラベルを含む箱**として扱い、力学のあとに重なりを解く。
  繋ぎのある曲だけで回し、孤立曲は下に整列させる
- **自動配置は「力学 → 点数で選ぶ → 交差を直接減らす」の3段**（`computeLayout`）。
  力学は近さしか知らないので線が交差したまま落ち着く（実測 56曲93繋ぎ: 力学だけで
  293交差。始点を12通り変えても275止まり。人が手で並べた `パターン1` でも110）。
  そこで **`scoreLayout`（交差＋線がラベルを踏んだ数）を物差しに、`refine` が
  1曲ずつ動かす・2曲を入れ替える**を「点が下がるときだけ」繰り返す → **41交差**。
  交差の最小化は NP困難なので**最適解ではない**。目的は「人の手より読める形を、
  押さずに出す」こと
- **点数の数え直しは動かした曲に関わる分だけ**（全部数え直すと11倍遅い）。
  幾何の下請け（`crossXY` / `hitsRect`）は**数値だけを受け取る** — 点や線分の
  オブジェクトを毎回作ると、その生成が計算時間を決めてしまう。
  交差だけを減らすと盤面が倍に広がるので、線の長さも軽く点数に入れる（`LENGTH_WEIGHT`）。
  ただし**広さ自体は縮まない** — 曲名の箱を重ねずにほどくには面積が要る
- **自動配置は重い（約0.7秒）ので、同じ曲・同じ繋ぎなら計算し直さない**
  （`computeLayout` 内のキャッシュ。鍵は並べ替えた曲IDと繋ぎID）。
  `/graph` は開くたびにサーバで描くため、これが無いと毎回0.7秒乗る
